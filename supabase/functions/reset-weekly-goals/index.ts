import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const supabaseUrl = Deno.env.get('SUPABASE_URL')!;
    const supabaseServiceKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
    
    const supabase = createClient(supabaseUrl, supabaseServiceKey);

    console.log('[RESET-WEEKLY-GOALS] Starting weekly goals reset');

    // Call the database function to reset weekly goals
    const { error } = await supabase.rpc('reset_weekly_goals');

    if (error) {
      console.error('[RESET-WEEKLY-GOALS] Error:', error);
      throw error;
    }

    console.log('[RESET-WEEKLY-GOALS] Weekly goals reset successfully');

    return new Response(
      JSON.stringify({ 
        success: true, 
        message: 'Weekly goals reset successfully' 
      }),
      { 
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
        status: 200 
      }
    );
  } catch (error) {
    console.error('[RESET-WEEKLY-GOALS] Error:', error);
    return new Response(
      JSON.stringify({ error: error.message }),
      { 
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
        status: 500 
      }
    );
  }
});